import React from 'react';
import SimpleHierarchyView from './SimpleHierarchyView';

/**
 * EditorView - Main editor with 3-column layout
 * This component now uses the React-based SimpleHierarchyView
 */
const EditorView: React.FC = () => {
  return <SimpleHierarchyView />;
};

export default EditorView;
